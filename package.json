{
  "name": "XRP2",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ rll-ctrl/XRP2",
    "create-local": "graph create --node http://localhost:8020/ rll-ctrl/XRP2",
    "remove-local": "graph remove --node http://localhost:8020/ rll-ctrl/XRP2",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 rll-ctrl/XRP2"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.20.1",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
